[tools]
# Can also use other package managers like npm, yarn, or pnpm
pnpm = { version = "10.10.0", postinstall = "pnpm install" }
# node = { version = "^22.0.0", postinstall = "npm install" }
# rust = {version = "1.86.0", postinstall = "cargo install tauri-cli --version '^2.0.0' --locked"}